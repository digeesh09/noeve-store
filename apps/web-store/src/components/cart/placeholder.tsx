'use client';

import React from 'react';
import Link from 'next/link';
import { CartBadge } from '@/components/cart/cart-badge';

export function CartBadge(): React.JSX.Element {
  // re-exported from cart-badge
  return <CartBadge />;
}
